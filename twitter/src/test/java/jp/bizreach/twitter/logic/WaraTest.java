package jp.bizreach.twitter.logic;

import jp.bizreach.twitter.dbflute.exbhv.MemberBhv;
import jp.bizreach.twitter.unit.UnitContainerTestCase;

public class WaraTest extends UnitContainerTestCase {
    public MemberBhv memberBhv;

    public void test_フォローしていない人をランダムで表示したい() throws Exception {
        // ## Arrange ##
        //        Integer myId = 1;
        //        MemberCB memberCB = new MemberCB();
        //        memberCB.setupSelect_FollowByYouIdAsOne();
        //        memberCB.query().setMemberId_NotEqual(myId);
        //        memberCB.query().setMemberId_NotInScope(followIdList);
        //        ListResultBean<Member> memberList = memberBhv.selectList(memberCB);
        //        for (Member member : memberList) {
        //            String userName = member.getUserName();
        //            followSuggestions.add(userName);
        //        }

        // ## Act ##

        // ## Assert ##
    }
}
